# 2026-09-19

## 项目通读（为后续功能需求做准备）
用户要求先完整理解 Hiko 项目架构、功能与默认规则，随后会提出功能需求。

已通读并消化的内容：
- `hiko/pubspec.yaml`（1.86.0+95，依赖与三平台能力）、`hiko/README.md`（功能清单 + 平台能力矩阵）
- `lib/main.dart`（启动序列：media_kit 初始化 → CoverCache → settings/library/categories 加载 → AudioSession → audio_service → 根层 BackgroundLayer + 全局快捷键 + ActivityOverlay）
- `lib/models/` 三个模型全量（Album.mergeWith 的旧数据继承策略、Track.lyricsText）
- `lib/data/` library_store（原子写 + 写队列）、library_provider（id + 曲目 URL 双路去重合并）、settings_store（全部字段与白名单归一化）、filter（9 种排序 + FilterAlbumsMemo）、stats
- `lib/playback/` playback_controller、playback_rules（QueueRules 纯函数）、platform_service 抽象
- `lib/ui/` home_screen 方法骨架、sidebar 导航项、theme 配色与玻璃 token
- `lib/utils/` rj / repair_text（GBK/Shift-JIS 打分还原）、`lib/lyrics/` 解析链路、`lib/data/dlsite_scraper.dart`、`lib/ui/covers/cover_cache.dart`
- `.zcode/plans/plan-hiko-flutter-rewrite.md` 的「已确认决策」「移植不丢需求」「明确不做」「项目结构」「里程碑」与 1.83–1.86 最新章节
- `hiko/BLOCKED.md` 既有待裁决清单

产出：`/Users/chenjh/Documents/ChatGPT/New project/.workbuddy/memory/MEMORY.md`（架构速查 + 9 条默认规则 + 既有待裁决）。
状态：仅阅读，未改动任何代码。工作区 clean（git status 0 条）。

## 用户确立的两条协作约定
1. 称呼不用管（不纠结叫法）。
2. **每次做关键决策时先用 grill-me 追问他**再动手。已写入：
   - `~/.workbuddy/MEMORY.md`（跨项目习惯）
   - 本项目的 `.workbuddy/memory/MEMORY.md` 默认规则第 0 条
   - `~/.workbuddy/SOUL.md` 新增一条 Core Truth「事实归我查，决策归他拍」
   - `~/.workbuddy/USER.md` 的 Context
身份文件（IDENTITY/USER/SOUL）已补全，BOOTSTRAP.md 已删除。

## 1.87.0 修复声优串分隔符不拆分（第一个功能需求，已发版）
用户报 bug：artist 标签 `柚木つばめ \ 逢坂成美` 在详情抽屉被当成一个人。
根因：`voiceSplitPattern` 白名单只有 `[、，,／/;；]`，缺反斜杠；且 album_detail_screen 留着 1.81 漏改的重复正则。

grill-me 五问答裁决：分隔符 B 档（加 `\` `＼` `|` `｜` `・` `･`）/ `・` 误伤照加 /
卡片胶囊+抽屉灰字行归一化成顿号不拆 / 筛选保持 `contains` / Android 重复实现统一到共享 util / 不改存储层。

改动：person_names.dart（扩白名单 + 新增 `normalizeVoiceSeparators`）、album_card.dart:171、
detail_drawer.dart:248、album_detail_screen.dart（删重复正则）、person_names_test.dart（3→7 条）。
版本 1.87.0+96；commit `5391ebe` 已推送；GitHub Release v1.87.0 双资产
（`hiko-v1.87.0-macos.zip` 31MB / `hiko-v1.87.0-android.apk` 63MB）。
验证：flutter test 288 passed/2 skipped（基线 284/2，净增 4）；analyze 39 与基线一致。

环境坑（已记入 MEMORY.md 规则 8）：跑 flutter test 必须 `env -u HTTP_PROXY ...` 摘代理，
否则 flutter_tester WebSocket 被代理吃掉导致 51 条集体 load 失败；flutter build macos 需沙箱放行且只能前台跑。

plan 文件已追加 1.87.0 章节（含裁决表与环境坑）。
